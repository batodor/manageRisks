<mvc:View
	controllerName="managerisks.controller.Detail"
	xmlns="sap.m"
	xmlns:l="sap.ui.layout"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core" 
	xmlns:semantic="sap.m.semantic"
	xmlns:custom="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
	xmlns:footerbar="sap.ushell.ui.footerbar">

	<semantic:DetailPage
		id="page"
		navButtonPress="onNavBack"
		showNavButton="{device>/system/phone}"
		title="{i18n>detailTitle}"
		busy="{detailView>/busy}"
		busyIndicatorDelay="{detailView>/delay}">

		<semantic:content>
			<ObjectHeader
				id="objectHeader"
				title="{Name}">
			</ObjectHeader>
			<IconTabBar id="itbMain" select="onTabSelected" selectedKey="dashboard">
				<items>
					<IconTabFilter text="{i18n>manageRisks}" key="risks">
						<core:Fragment fragmentName="fragment.risks" type="XML"></core:Fragment>
					</IconTabFilter>
					<IconTabFilter text="{i18n>manageLimits}" key="limits">
						<core:Fragment fragmentName="fragment.limits" type="XML"></core:Fragment>
					</IconTabFilter>
				</items>
			</IconTabBar>
		</semantic:content>
		
		<semantic:mainAction>
			<semantic:MainAction
				press="openDeal"
				text="{i18n>openDeal}" />
		</semantic:mainAction>
		<semantic:saveAsTileAction>
			<Button
				press="save"
				type="Accept"
				text="{i18n>save}" />
		</semantic:saveAsTileAction>
		<semantic:positiveAction>
			<semantic:PositiveAction
				press="approve"
				text="{i18n>approve}" />
		</semantic:positiveAction>
		
	</semantic:DetailPage>

</mvc:View>